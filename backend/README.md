# Backend Directory

## Setup
These are the commands on installing the right Python dependencies to run the project:

`pip install -r requirements.txt`

<br>

## Running the Project
The command for running the project is very simple:

`python app.py`

